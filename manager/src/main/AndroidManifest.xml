<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="com.yukista.tutaua.permission.MANAGE_BOX" />
    <queries>
        <package android:name="com.yukista.tutaua.manager" />
        <package android:name="com.yukista.tutaua.box" />
        <package android:name="com.yukista.tutaua" />
        <package android:name="tv.tutaua.app" />
        <package android:name="com.yukista.tutaua.games" />
    </queries>
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:label="@string/app_name"
        android:usesCleartextTraffic="false">
        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".ProvisioningReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter><action android:name="com.yukista.tutaua.manager.action.PROVISION" /></intent-filter>
        </receiver>
        <activity
            android:name=".BootstrapActivity"
            android:exported="true"
            android:permission="android.permission.DUMP"
            android:theme="@android:style/Theme.NoDisplay" />
        <service
            android:name=".SyncJobService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />
    </application>
</manifest>
